<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace命名空间，作用就是对sql进行分类化管理，理解sql隔离
注意，使用mapper代理进行开发，namespace有特殊重要的作用 -->
<mapper namespace="test">

	<!-- 在映射文件中配置很多sql语句 -->
	<!-- 需求：通过id查询用户表记录 -->
	<!-- 通过select执行数据库查询
	id：标识映射文件中的sql
	将sql语句封装到mappedStatement对象中，所以将id成为statement的id 
	parameterType:指定输入参数的类型，这里指定的是int
	resultType:输出结果的类型.指定sql输出结果的所映射的java对象类型；select指定resultType表示将单条记录映射成的java对象
	#{}标识一个占位符
	#{id}：其中的id表示接受输入的参数，其名称就是id，如果输入参数的类型是简单类型，#{}中的参数名可以任意，可以是value或者其他名称
	-->
	<select id="findUserById" parameterType="int" resultType="com.zeek.mybatis3.po.User">
		SELECT * FROM USER WHERE ID = #{id}
	</select>
	


</mapper>
